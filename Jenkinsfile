@Library("jenkins-share")_
pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                  echo "hi pipeline"
                  Global_vars "info message"
                  Global_vars "warning message"
                  
            }     
            
        }
    }
}
