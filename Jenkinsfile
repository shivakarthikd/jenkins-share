@Library("jenkins-share")_
pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                  echo "hi pipeline"
                  globalVars "info message"
                  globalVars "warning message"
                  
            }     
            
        }
    }
}
