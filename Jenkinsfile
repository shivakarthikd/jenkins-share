@Library("jenkins-shared")


pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                  echo "hi pipeline"
                  foo.info 'info message'
                  foo.warning 'warning message'


            }
        }
    }
}